/**
 * Returns the keys and values of the input object
 * @param {object[]} data 
 * @returns {string[][]}
 */
function keysAndValues(data){
    return [Object.keys(data), Object.values(data)];
}