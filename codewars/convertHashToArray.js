/**
 * Returns an array with the properties and values of the input object
 * @param {object} hash 
 * @returns {string[]}
 */
function convertHashToArray(hash){
    return  Object.entries(hash);
}