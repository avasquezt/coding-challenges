"use strict";

/**
 * Flattens and sorts the input array
 * @param {number[][]} array 
 * @returns {number[]}
 */
function flattenAndSort(array) {
  // Good luck, brave code warrior!
  return array.flat().sort((a, b) => a - b);
}